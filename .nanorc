set atblanks
set autoindent
set cutfromcursor
set softwrap
set tabsize 2

# Syntax highlighting is appended at install time. Nano 9.0+ rejects `include`
# inside an included file, so we can't use indirection -- the install script
# writes ~/.nanorc as a copy of this file with a platform-specific include
# line appended.
